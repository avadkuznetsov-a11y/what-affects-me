__pycache__/
*.pyc
.pytest_cache/
.venv/
.env

# ключи для локального запуска, в репозиторий не попадают
.env
.envrc

# Служебное: мусор macOS и рабочая память агентов - людям, которые скачают
# репозиторий, это не нужно
.DS_Store
.claude/agent-memory/

# Черновики вида страницы: отвергнутые наброски и собранный холст на 2 МБ.
# Исходные артборды рядом лежат и весят килобайты - их держим.
docs/mockups/
docs/design/mira-interface.html

# Дневник человека. В репозиторий он не попадает никогда: это самое личное, что
# есть у того, кто запустил программу. Рядом - файл, отложенный при поломке.
.diary.db
.diary.db-journal
.diary.db.broken
# Город хранился отдельным файлом, пока не переехал в дневник. У тех, кто
# запускал прототип раньше, файл ещё лежит на диске - пусть лежит незаметно.
web/.city
